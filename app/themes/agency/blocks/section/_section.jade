mixin section(secid,seclass,heading,subheading)
	section(id=secid class=seclass)
		.container
			.row
				div(class="col-lg-12 text-center")
					h2(class="section-heading")=heading
					h3(class="section-subheading text-muted")=subheading